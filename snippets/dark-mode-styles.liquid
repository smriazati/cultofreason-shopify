<style>
  body:not(.template-index) {
    background: var(--volcano);
  }
  .site-header a, 
  .site-header button,
  .site-main,
  .page-container a:not(.link-plain),
  .site-footer,
  .site-footer nav ul a,
  .site-footer .text-footer-newsletter-text,
  .site-footer input::placeholder,
  .site-footer .btn-square {
    color: #FFF;
  }
  .site-header li:hover {
    border-color: #fff;
  }
  .site-header img.brand {
    filter: invert(1);
  }
  .site-footer input {
    border-bottom: 1px solid #FFF;
  }
  .site-footer .btn-square {
    border: 1px solid #FFF;
  }
</style>